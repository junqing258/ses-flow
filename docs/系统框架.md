# 系统框架

## 一、容器图（Container Diagram）

```mermaid
graph TB
    user["👤 用户 / 开发者\n在浏览器中编辑工作流、触发运行、查看结果"]

    subgraph ses["SES Flow Monorepo"]
        frontend["Frontend SPA\nVue 3 + Vite · apps/frontend\n工作流可视化编辑器，运行监控面板"]
        backend["Backend Service\nRust + Axum · apps/backend :6302\n统一对外入口：静态资源托管 / REST API / SSE / 登录认证 / AI 代理"]
        runner_lib["Runner Library\nRust crate · apps/runner\n嵌入 backend 进程，负责工作流调度与执行\nWorkflowApp / WorkflowEngine / Executors\n并发控制（ConcurrencyGate）"]
        ai_gateway["AI Gateway\nNode.js + Express · apps/ai-gateway :6307\n管理 Claude AI 会话、SSE 事件流、工具权限"]
        postgres[("PostgreSQL\n存储工作流 catalog、运行记录\n编辑 session、运行快照")]
    end

    subgraph plugin_apps["Plugin Apps"]
        workstation_plugin["workstation-plugin\n与工作台 App 交互\n上报任务状态、接收工作台指令"]
        rcs_plugin["RCS-plugin\n与 RCS 及 RCS App 交互\n下发调度指令、接收 RCS 回调事件"]
    end

    claude_sdk["Claude Agent SDK\nAnthropic AI Agent，受限工具调用"]
    node_proc["Node.js 子进程\ncode executor 按需拉起（JS/TS fork）"]
    wasm_runtime["Wasmtime 运行时\ncode executor 内嵌；加载预编译 .wasm 模块\nRust 离线编译 → wasm32-wasip1，沙箱隔离，< 5ms"]

    workstation_app["工作台 App\n现场作业终端"]
    rcs["RCS / RCS App\n机器人控制系统及其移动端"]

    user -- "编辑工作流 / 查看运行结果 · HTTPS" --> frontend
    frontend -- "工作流 CRUD / 触发运行 / SSE 订阅\nHTTP /runner-api" --> backend
    frontend -- "AI 协作消息 / 事件流\nHTTP /api/ai/*" --> backend
    backend -- "托管 SPA 静态资源 · ServeDir /views" --> frontend
    backend -- "反向代理 AI 请求\nHTTP /api/ai/* → :6307" --> ai_gateway
    ai_gateway -- "回调读写 edit session / preview\nHTTP /runner-api" --> backend
    ai_gateway -- "调用 Claude Agent SDK · HTTPS API" --> claude_sdk
    backend -- "调用（库函数，同进程）· in-process" --> runner_lib
    runner_lib -- "读写 catalog / 运行记录 / edit session / 快照\nSQL via sqlx" --> postgres
    runner_lib -- "code 节点 JS/TS：fork 子进程\nstdin/stdout" --> node_proc
    runner_lib -- "code 节点 Rust：加载 .wasm 模块\nin-process" --> wasm_runtime
    runner_lib -- "task 节点回调 / TaskHandler 注册\nHTTP / in-process" --> workstation_plugin
    runner_lib -- "task 节点回调 / TaskHandler 注册\nHTTP / in-process" --> rcs_plugin
    workstation_plugin -- "推送任务 / 接收操作结果\nHTTP / WebSocket" --> workstation_app
    rcs_plugin -- "下发调度指令 / 接收 RCS 事件\nHTTP / WebSocket" --> rcs
```

---

## 二、Runner 节点执行设计（Component Diagram）

```mermaid
graph TB
    backend_api["Backend Service\nRust + Axum\n接收 HTTP 请求，触发工作流运行 / 恢复 / 终止"]

    subgraph runner["Runner Library  (apps/runner)"]
        workflow_app["WorkflowApp\n对外公开 API：run / resume / terminate / edit-session\n持有 ConcurrencyGate，管理并发配额"]
        concurrency_gate["ConcurrencyGate\nSemaphore + Mutex\n全局最大并发（默认 50）+ 每工作流最大并发（默认 5）\nOverflowPolicy：Queue / Reject"]
        workflow_runner["WorkflowRunner\n组装 WorkflowEngine + RunStore\n执行后持久化 summary 与 snapshot"]
        workflow_engine["WorkflowEngine\n逐节点驱动执行循环\n读取 TransitionDefinition 决定下一节点\n调用 Observer / Controller 钩子"]
        executor_registry["ExecutorRegistry\nHashMap&lt;NodeType, Arc&lt;dyn NodeExecutor&gt;&gt;\n按 NodeType 注册并分发执行器\n支持运行时注册自定义执行器"]
        node_executor_trait["NodeExecutor trait\nfn execute(node, context) -> Result&lt;NodeExecutionResult&gt;\nNodeExecutionResult 携带：status / output / state_patch\n/ branch_key / next_signal / logs / terminal"]
        node_context["NodeExecutionContext\n每次节点调用的只读上下文快照\nrun_id / workflow_key / trigger / input\n/ state / env / WorkflowRunController"]

        subgraph executors["节点执行器"]
            exec_trigger["触发器节点\nstart / webhook_trigger\nstart：提取触发输入，初始化 state\nwebhook_trigger：提取 trigger payload 传入下游"]
            exec_flow["流程控制节点\nif_else / switch\nif_else：对 condition 求值，输出 branch_key\nswitch：对 expression 求值，匹配 case"]
            exec_data["数据操作节点\nset_state / fetch\nset_state：outputMapping 合并写入 state\nfetch：发起 HTTP 请求，返回 response + data"]
            exec_async["异步等待节点\nwait / task / respond\nwait：挂起等待外部 resume\ntask：调用 TaskHandler.create()，挂起等待\nrespond：向 webhook 调用方回写 HTTP 响应"]
            exec_compute["计算节点\ncode\nJS/TS → fork Node.js 子进程\nRust → Wasmtime 内嵌加载 .wasm（< 5ms）\n支持 timeout + terminate 信号"]
            exec_sub["子流程节点\nsub_workflow\n内联或按 ref 解析子工作流定义\n递归调用 WorkflowEngine.run()\n子流程 Waiting 时透传 NextSignal 给父流程"]
            exec_end["终止节点\nend\n标记流程结束，输出最终 state\nterminal=true"]
        end

        store_catalog["CatalogStore\ntrait + PG/InMem 双实现\n存取 WorkflowDefinition\n支持 sub_workflow ref 解析"]
        store_run["RunStore\ntrait + PG/InMem 双实现\n存取 WorkflowRunRecord + Snapshot\n恢复执行时读取 snapshot"]
        store_session["EditSessionStore\ntrait + PG/InMem 双实现\n存取编辑草稿\nAI 协作写入 draft，Frontend SSE 订阅变更"]
        task_handler_registry["TaskHandlerRegistry\nHashMap&lt;String, Arc&lt;dyn TaskHandler&gt;&gt;\ntask 节点委托的业务处理器注册表\n内置 mock manual_review；可注册领域 handler"]
        workflow_def_registry["WorkflowDefinitionRegistry\nHashMap&lt;String, WorkflowDefinition&gt;\n供 sub_workflow ref 查找的内存定义注册表"]
    end

    postgres[("PostgreSQL\nPG 实现的持久化后端")]
    node_proc["Node.js 子进程\ncode executor JS/TS fork"]
    wasm_runtime["Wasmtime 运行时\n内嵌于 runner 进程；加载预编译 .wasm 模块\n模块缓存（首次编译后 < 5ms）；沙箱隔离"]

    backend_api -- "run / resume / terminate\nin-process fn call" --> workflow_app
    workflow_app -- "acquire permit（global + per-workflow）\nSemaphore.acquire" --> concurrency_gate
    workflow_app -- "委托执行 · in-process" --> workflow_runner
    workflow_runner -- "engine.run() / engine.resume()\nin-process" --> workflow_engine
    workflow_runner -- "持久化 summary / snapshot" --> store_run
    workflow_engine -- "registry.resolve(node_type)" --> executor_registry
    workflow_engine -- "构造 NodeExecutionContext 传入每个 executor" --> node_context
    executor_registry -- "调度执行 · trait dispatch" --> node_executor_trait
    node_executor_trait --> exec_trigger
    node_executor_trait --> exec_flow
    node_executor_trait --> exec_data
    node_executor_trait --> exec_async
    node_executor_trait --> exec_compute
    node_executor_trait --> exec_sub
    node_executor_trait --> exec_end
    exec_data -- "解析外部服务地址（WorkflowServices）" --> store_catalog
    exec_sub -- "按 ref 查找子工作流定义" --> workflow_def_registry
    exec_async -- "task 节点 → handler.create(payload)" --> task_handler_registry
    exec_compute -- "JS/TS：fork + stdin/stdout + timeout/terminate" --> node_proc
    exec_compute -- "Rust：加载 .wasm（模块缓存）+ epoch 超时 / terminate" --> wasm_runtime
    store_catalog -- "SQL · sqlx" --> postgres
    store_run -- "SQL · sqlx" --> postgres
    store_session -- "SQL · sqlx" --> postgres
```

### NodeExecutionResult 语义

| `status` | 含义 |
|---|---|
| `success` | 继续执行下一节点 |
| `waiting` | 挂起；保存 snapshot；等待 `resume(run_id, signal)` |
| `failed` | 触发 `onError` 策略或终止 |

- **`terminal=true`**：无论 status，立即结束工作流
- **`branch_key`**：传给 `Transition.condition` 决定分支
- **`state_patch`**：merge 到全局 workflow state
- **`next_signal`**：异步节点产出，随 SSE 推送给前端

### 恢复流程（resume）

1. 从 `RunStore` 读取 `WorkflowRunSnapshot`
2. snapshot 包含：`currentNodeId` / `trigger` / `lastInput` / `state` / `timeline` / `lastSignal` / `env`
3. `WorkflowEngine` 从 `currentNodeId` 继续执行
4. 支持手动 patch（manual-patch API）修改恢复时注入的 input

---

## 三、Plugin Apps

Plugin Apps 是挂载在 Runner 侧的领域插件，通过实现 `TaskHandler` trait 注册到 `TaskHandlerRegistry`，由 `task` 节点在工作流执行时委托调用。

| Plugin | 职责 | 对接外部系统 |
|---|---|---|
| **workstation-plugin** | 与工作台 App 交互：向现场作业终端推送任务、接收操作员确认/拒绝结果，将结果作为 `resume signal` 回写 Runner | 工作台 App（现场终端） |
| **RCS-plugin** | 与 RCS 及 RCS App 交互：向机器人控制系统下发调度指令、监听 RCS 回调事件，将执行结果作为 `resume signal` 回写 Runner | RCS / RCS App（机器人控制系统及移动端） |

### 典型交互流程

```
WorkflowEngine
  └─ task 节点 → TaskHandlerRegistry.dispatch(handler_key, payload)
       ├─ workstation-plugin.create(payload)
       │     → 推送任务到工作台 App
       │     → 工作台 App 操作后回调 plugin
       │     → plugin 调用 Runner resume(run_id, signal)
       └─ rcs-plugin.create(payload)
             → 下发调度指令到 RCS
             → RCS 执行完毕后回调 plugin
             → plugin 调用 Runner resume(run_id, signal)
```
