interface:
  display_name: "SES Flow Skill"
  short_description: "Use runner_base_url and session_id to push AI draft updates"
  default_prompt: "Use $ses-flow-skill to edit an SES Flow workflow through runner edit sessions, providing runner_base_url and session_id to refresh the AI preview."

policy:
  allow_implicit_invocation: true
