[package]
name = "runner"
version = "0.1.0"
edition = "2024"
description = "Self-hosted DSL runner for the SES workflow platform"
license = "UNLICENSED"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "2.0"
